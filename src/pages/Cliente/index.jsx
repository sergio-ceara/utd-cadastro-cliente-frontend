import { Outlet } from "react-router-dom";
import styles from './Cliente.module.css';
function Cliente(){
 
    return(
        <>

            <Outlet />
        </>
    );
}

export default Cliente;
